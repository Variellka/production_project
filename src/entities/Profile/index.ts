import { ProfileSchema, ProfileType } from './model/types/profile';
import { profileActions, profileReducer } from './model/slice/profileSlice';

export {
    ProfileSchema, ProfileType, profileActions, profileReducer,
};
